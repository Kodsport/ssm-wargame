wargame.movitz.dev {
	encode gzip

	handle /api/* {	
		uri strip_prefix /api
		reverse_proxy api:8000
	}
	
	handle /ssm-bucket/* {
		reverse_proxy minio:9000
	}

	handle {
		root * /var/www/
		try_files {path} /index.html
		file_server
	}
}
