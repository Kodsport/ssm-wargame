// Code generated by SQLBoiler 4.14.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CTFTeam is an object representing the database table.
type CTFTeam struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	CTFID     string    `boil:"ctf_id" json:"ctf_id" toml:"ctf_id" yaml:"ctf_id"`
	Teamname  string    `boil:"teamname" json:"teamname" toml:"teamname" yaml:"teamname"`
	Password  string    `boil:"password" json:"password" toml:"password" yaml:"password"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *ctfTeamR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ctfTeamL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CTFTeamColumns = struct {
	ID        string
	CTFID     string
	Teamname  string
	Password  string
	CreatedAt string
}{
	ID:        "id",
	CTFID:     "ctf_id",
	Teamname:  "teamname",
	Password:  "password",
	CreatedAt: "created_at",
}

var CTFTeamTableColumns = struct {
	ID        string
	CTFID     string
	Teamname  string
	Password  string
	CreatedAt string
}{
	ID:        "ctf_teams.id",
	CTFID:     "ctf_teams.ctf_id",
	Teamname:  "ctf_teams.teamname",
	Password:  "ctf_teams.password",
	CreatedAt: "ctf_teams.created_at",
}

// Generated where

var CTFTeamWhere = struct {
	ID        whereHelperstring
	CTFID     whereHelperstring
	Teamname  whereHelperstring
	Password  whereHelperstring
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"ctf_teams\".\"id\""},
	CTFID:     whereHelperstring{field: "\"ctf_teams\".\"ctf_id\""},
	Teamname:  whereHelperstring{field: "\"ctf_teams\".\"teamname\""},
	Password:  whereHelperstring{field: "\"ctf_teams\".\"password\""},
	CreatedAt: whereHelpertime_Time{field: "\"ctf_teams\".\"created_at\""},
}

// CTFTeamRels is where relationship names are stored.
var CTFTeamRels = struct {
	CTF           string
	TeamCTFSolves string
	TeamCTFUsers  string
}{
	CTF:           "CTF",
	TeamCTFSolves: "TeamCTFSolves",
	TeamCTFUsers:  "TeamCTFUsers",
}

// ctfTeamR is where relationships are stored.
type ctfTeamR struct {
	CTF           *CTF         `boil:"CTF" json:"CTF" toml:"CTF" yaml:"CTF"`
	TeamCTFSolves CTFSolfSlice `boil:"TeamCTFSolves" json:"TeamCTFSolves" toml:"TeamCTFSolves" yaml:"TeamCTFSolves"`
	TeamCTFUsers  CTFUserSlice `boil:"TeamCTFUsers" json:"TeamCTFUsers" toml:"TeamCTFUsers" yaml:"TeamCTFUsers"`
}

// NewStruct creates a new relationship struct
func (*ctfTeamR) NewStruct() *ctfTeamR {
	return &ctfTeamR{}
}

func (r *ctfTeamR) GetCTF() *CTF {
	if r == nil {
		return nil
	}
	return r.CTF
}

func (r *ctfTeamR) GetTeamCTFSolves() CTFSolfSlice {
	if r == nil {
		return nil
	}
	return r.TeamCTFSolves
}

func (r *ctfTeamR) GetTeamCTFUsers() CTFUserSlice {
	if r == nil {
		return nil
	}
	return r.TeamCTFUsers
}

// ctfTeamL is where Load methods for each relationship are stored.
type ctfTeamL struct{}

var (
	ctfTeamAllColumns            = []string{"id", "ctf_id", "teamname", "password", "created_at"}
	ctfTeamColumnsWithoutDefault = []string{"id", "ctf_id", "teamname", "password"}
	ctfTeamColumnsWithDefault    = []string{"created_at"}
	ctfTeamPrimaryKeyColumns     = []string{"id"}
	ctfTeamGeneratedColumns      = []string{}
)

type (
	// CTFTeamSlice is an alias for a slice of pointers to CTFTeam.
	// This should almost always be used instead of []CTFTeam.
	CTFTeamSlice []*CTFTeam
	// CTFTeamHook is the signature for custom CTFTeam hook methods
	CTFTeamHook func(context.Context, boil.ContextExecutor, *CTFTeam) error

	ctfTeamQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ctfTeamType                 = reflect.TypeOf(&CTFTeam{})
	ctfTeamMapping              = queries.MakeStructMapping(ctfTeamType)
	ctfTeamPrimaryKeyMapping, _ = queries.BindMapping(ctfTeamType, ctfTeamMapping, ctfTeamPrimaryKeyColumns)
	ctfTeamInsertCacheMut       sync.RWMutex
	ctfTeamInsertCache          = make(map[string]insertCache)
	ctfTeamUpdateCacheMut       sync.RWMutex
	ctfTeamUpdateCache          = make(map[string]updateCache)
	ctfTeamUpsertCacheMut       sync.RWMutex
	ctfTeamUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ctfTeamAfterSelectHooks []CTFTeamHook

var ctfTeamBeforeInsertHooks []CTFTeamHook
var ctfTeamAfterInsertHooks []CTFTeamHook

var ctfTeamBeforeUpdateHooks []CTFTeamHook
var ctfTeamAfterUpdateHooks []CTFTeamHook

var ctfTeamBeforeDeleteHooks []CTFTeamHook
var ctfTeamAfterDeleteHooks []CTFTeamHook

var ctfTeamBeforeUpsertHooks []CTFTeamHook
var ctfTeamAfterUpsertHooks []CTFTeamHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CTFTeam) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CTFTeam) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CTFTeam) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CTFTeam) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CTFTeam) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CTFTeam) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CTFTeam) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CTFTeam) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CTFTeam) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ctfTeamAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCTFTeamHook registers your hook function for all future operations.
func AddCTFTeamHook(hookPoint boil.HookPoint, ctfTeamHook CTFTeamHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ctfTeamAfterSelectHooks = append(ctfTeamAfterSelectHooks, ctfTeamHook)
	case boil.BeforeInsertHook:
		ctfTeamBeforeInsertHooks = append(ctfTeamBeforeInsertHooks, ctfTeamHook)
	case boil.AfterInsertHook:
		ctfTeamAfterInsertHooks = append(ctfTeamAfterInsertHooks, ctfTeamHook)
	case boil.BeforeUpdateHook:
		ctfTeamBeforeUpdateHooks = append(ctfTeamBeforeUpdateHooks, ctfTeamHook)
	case boil.AfterUpdateHook:
		ctfTeamAfterUpdateHooks = append(ctfTeamAfterUpdateHooks, ctfTeamHook)
	case boil.BeforeDeleteHook:
		ctfTeamBeforeDeleteHooks = append(ctfTeamBeforeDeleteHooks, ctfTeamHook)
	case boil.AfterDeleteHook:
		ctfTeamAfterDeleteHooks = append(ctfTeamAfterDeleteHooks, ctfTeamHook)
	case boil.BeforeUpsertHook:
		ctfTeamBeforeUpsertHooks = append(ctfTeamBeforeUpsertHooks, ctfTeamHook)
	case boil.AfterUpsertHook:
		ctfTeamAfterUpsertHooks = append(ctfTeamAfterUpsertHooks, ctfTeamHook)
	}
}

// One returns a single ctfTeam record from the query.
func (q ctfTeamQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CTFTeam, error) {
	o := &CTFTeam{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ctf_teams")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CTFTeam records from the query.
func (q ctfTeamQuery) All(ctx context.Context, exec boil.ContextExecutor) (CTFTeamSlice, error) {
	var o []*CTFTeam

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CTFTeam slice")
	}

	if len(ctfTeamAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CTFTeam records in the query.
func (q ctfTeamQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ctf_teams rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ctfTeamQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ctf_teams exists")
	}

	return count > 0, nil
}

// CTF pointed to by the foreign key.
func (o *CTFTeam) CTF(mods ...qm.QueryMod) ctfQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CTFID),
	}

	queryMods = append(queryMods, mods...)

	return CTFS(queryMods...)
}

// TeamCTFSolves retrieves all the ctf_solf's CTFSolves with an executor via team_id column.
func (o *CTFTeam) TeamCTFSolves(mods ...qm.QueryMod) ctfSolfQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"ctf_solves\".\"team_id\"=?", o.ID),
	)

	return CTFSolves(queryMods...)
}

// TeamCTFUsers retrieves all the ctf_user's CTFUsers with an executor via team_id column.
func (o *CTFTeam) TeamCTFUsers(mods ...qm.QueryMod) ctfUserQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"ctf_users\".\"team_id\"=?", o.ID),
	)

	return CTFUsers(queryMods...)
}

// LoadCTF allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ctfTeamL) LoadCTF(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCTFTeam interface{}, mods queries.Applicator) error {
	var slice []*CTFTeam
	var object *CTFTeam

	if singular {
		var ok bool
		object, ok = maybeCTFTeam.(*CTFTeam)
		if !ok {
			object = new(CTFTeam)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCTFTeam))
			}
		}
	} else {
		s, ok := maybeCTFTeam.(*[]*CTFTeam)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCTFTeam))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &ctfTeamR{}
		}
		args = append(args, object.CTFID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ctfTeamR{}
			}

			for _, a := range args {
				if a == obj.CTFID {
					continue Outer
				}
			}

			args = append(args, obj.CTFID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`ctfs`),
		qm.WhereIn(`ctfs.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CTF")
	}

	var resultSlice []*CTF
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CTF")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ctfs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ctfs")
	}

	if len(ctfAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CTF = foreign
		if foreign.R == nil {
			foreign.R = &ctfR{}
		}
		foreign.R.CTFTeams = append(foreign.R.CTFTeams, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CTFID == foreign.ID {
				local.R.CTF = foreign
				if foreign.R == nil {
					foreign.R = &ctfR{}
				}
				foreign.R.CTFTeams = append(foreign.R.CTFTeams, local)
				break
			}
		}
	}

	return nil
}

// LoadTeamCTFSolves allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ctfTeamL) LoadTeamCTFSolves(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCTFTeam interface{}, mods queries.Applicator) error {
	var slice []*CTFTeam
	var object *CTFTeam

	if singular {
		var ok bool
		object, ok = maybeCTFTeam.(*CTFTeam)
		if !ok {
			object = new(CTFTeam)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCTFTeam))
			}
		}
	} else {
		s, ok := maybeCTFTeam.(*[]*CTFTeam)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCTFTeam))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &ctfTeamR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ctfTeamR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ID) {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`ctf_solves`),
		qm.WhereIn(`ctf_solves.team_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ctf_solves")
	}

	var resultSlice []*CTFSolf
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ctf_solves")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ctf_solves")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ctf_solves")
	}

	if len(ctfSolfAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamCTFSolves = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ctfSolfR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.TeamID) {
				local.R.TeamCTFSolves = append(local.R.TeamCTFSolves, foreign)
				if foreign.R == nil {
					foreign.R = &ctfSolfR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamCTFUsers allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ctfTeamL) LoadTeamCTFUsers(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCTFTeam interface{}, mods queries.Applicator) error {
	var slice []*CTFTeam
	var object *CTFTeam

	if singular {
		var ok bool
		object, ok = maybeCTFTeam.(*CTFTeam)
		if !ok {
			object = new(CTFTeam)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCTFTeam))
			}
		}
	} else {
		s, ok := maybeCTFTeam.(*[]*CTFTeam)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCTFTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCTFTeam))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &ctfTeamR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ctfTeamR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ID) {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`ctf_users`),
		qm.WhereIn(`ctf_users.team_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ctf_users")
	}

	var resultSlice []*CTFUser
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ctf_users")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ctf_users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ctf_users")
	}

	if len(ctfUserAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamCTFUsers = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ctfUserR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.TeamID) {
				local.R.TeamCTFUsers = append(local.R.TeamCTFUsers, foreign)
				if foreign.R == nil {
					foreign.R = &ctfUserR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// SetCTF of the ctfTeam to the related item.
// Sets o.R.CTF to related.
// Adds o to related.R.CTFTeams.
func (o *CTFTeam) SetCTF(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CTF) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"ctf_teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"ctf_id"}),
		strmangle.WhereClause("\"", "\"", 2, ctfTeamPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CTFID = related.ID
	if o.R == nil {
		o.R = &ctfTeamR{
			CTF: related,
		}
	} else {
		o.R.CTF = related
	}

	if related.R == nil {
		related.R = &ctfR{
			CTFTeams: CTFTeamSlice{o},
		}
	} else {
		related.R.CTFTeams = append(related.R.CTFTeams, o)
	}

	return nil
}

// AddTeamCTFSolves adds the given related objects to the existing relationships
// of the ctf_team, optionally inserting them as new records.
// Appends related to o.R.TeamCTFSolves.
// Sets related.R.Team appropriately.
func (o *CTFTeam) AddTeamCTFSolves(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CTFSolf) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TeamID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"ctf_solves\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, ctfSolfPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TeamID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &ctfTeamR{
			TeamCTFSolves: related,
		}
	} else {
		o.R.TeamCTFSolves = append(o.R.TeamCTFSolves, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ctfSolfR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// SetTeamCTFSolves removes all previously related items of the
// ctf_team replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Team's TeamCTFSolves accordingly.
// Replaces o.R.TeamCTFSolves with related.
// Sets related.R.Team's TeamCTFSolves accordingly.
func (o *CTFTeam) SetTeamCTFSolves(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CTFSolf) error {
	query := "update \"ctf_solves\" set \"team_id\" = null where \"team_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TeamCTFSolves {
			queries.SetScanner(&rel.TeamID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Team = nil
		}
		o.R.TeamCTFSolves = nil
	}

	return o.AddTeamCTFSolves(ctx, exec, insert, related...)
}

// RemoveTeamCTFSolves relationships from objects passed in.
// Removes related items from R.TeamCTFSolves (uses pointer comparison, removal does not keep order)
// Sets related.R.Team.
func (o *CTFTeam) RemoveTeamCTFSolves(ctx context.Context, exec boil.ContextExecutor, related ...*CTFSolf) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TeamID, nil)
		if rel.R != nil {
			rel.R.Team = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("team_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TeamCTFSolves {
			if rel != ri {
				continue
			}

			ln := len(o.R.TeamCTFSolves)
			if ln > 1 && i < ln-1 {
				o.R.TeamCTFSolves[i] = o.R.TeamCTFSolves[ln-1]
			}
			o.R.TeamCTFSolves = o.R.TeamCTFSolves[:ln-1]
			break
		}
	}

	return nil
}

// AddTeamCTFUsers adds the given related objects to the existing relationships
// of the ctf_team, optionally inserting them as new records.
// Appends related to o.R.TeamCTFUsers.
// Sets related.R.Team appropriately.
func (o *CTFTeam) AddTeamCTFUsers(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CTFUser) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TeamID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"ctf_users\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, ctfUserPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TeamID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &ctfTeamR{
			TeamCTFUsers: related,
		}
	} else {
		o.R.TeamCTFUsers = append(o.R.TeamCTFUsers, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ctfUserR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// SetTeamCTFUsers removes all previously related items of the
// ctf_team replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Team's TeamCTFUsers accordingly.
// Replaces o.R.TeamCTFUsers with related.
// Sets related.R.Team's TeamCTFUsers accordingly.
func (o *CTFTeam) SetTeamCTFUsers(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CTFUser) error {
	query := "update \"ctf_users\" set \"team_id\" = null where \"team_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TeamCTFUsers {
			queries.SetScanner(&rel.TeamID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Team = nil
		}
		o.R.TeamCTFUsers = nil
	}

	return o.AddTeamCTFUsers(ctx, exec, insert, related...)
}

// RemoveTeamCTFUsers relationships from objects passed in.
// Removes related items from R.TeamCTFUsers (uses pointer comparison, removal does not keep order)
// Sets related.R.Team.
func (o *CTFTeam) RemoveTeamCTFUsers(ctx context.Context, exec boil.ContextExecutor, related ...*CTFUser) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TeamID, nil)
		if rel.R != nil {
			rel.R.Team = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("team_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TeamCTFUsers {
			if rel != ri {
				continue
			}

			ln := len(o.R.TeamCTFUsers)
			if ln > 1 && i < ln-1 {
				o.R.TeamCTFUsers[i] = o.R.TeamCTFUsers[ln-1]
			}
			o.R.TeamCTFUsers = o.R.TeamCTFUsers[:ln-1]
			break
		}
	}

	return nil
}

// CTFTeams retrieves all the records using an executor.
func CTFTeams(mods ...qm.QueryMod) ctfTeamQuery {
	mods = append(mods, qm.From("\"ctf_teams\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"ctf_teams\".*"})
	}

	return ctfTeamQuery{q}
}

// FindCTFTeam retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCTFTeam(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*CTFTeam, error) {
	ctfTeamObj := &CTFTeam{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"ctf_teams\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, ctfTeamObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ctf_teams")
	}

	if err = ctfTeamObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ctfTeamObj, err
	}

	return ctfTeamObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CTFTeam) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ctf_teams provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ctfTeamColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ctfTeamInsertCacheMut.RLock()
	cache, cached := ctfTeamInsertCache[key]
	ctfTeamInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ctfTeamAllColumns,
			ctfTeamColumnsWithDefault,
			ctfTeamColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ctfTeamType, ctfTeamMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ctfTeamType, ctfTeamMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"ctf_teams\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"ctf_teams\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ctf_teams")
	}

	if !cached {
		ctfTeamInsertCacheMut.Lock()
		ctfTeamInsertCache[key] = cache
		ctfTeamInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CTFTeam.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CTFTeam) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ctfTeamUpdateCacheMut.RLock()
	cache, cached := ctfTeamUpdateCache[key]
	ctfTeamUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ctfTeamAllColumns,
			ctfTeamPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ctf_teams, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"ctf_teams\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, ctfTeamPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ctfTeamType, ctfTeamMapping, append(wl, ctfTeamPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ctf_teams row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ctf_teams")
	}

	if !cached {
		ctfTeamUpdateCacheMut.Lock()
		ctfTeamUpdateCache[key] = cache
		ctfTeamUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ctfTeamQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ctf_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ctf_teams")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CTFTeamSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ctfTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"ctf_teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, ctfTeamPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ctfTeam slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ctfTeam")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CTFTeam) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ctf_teams provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ctfTeamColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ctfTeamUpsertCacheMut.RLock()
	cache, cached := ctfTeamUpsertCache[key]
	ctfTeamUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			ctfTeamAllColumns,
			ctfTeamColumnsWithDefault,
			ctfTeamColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ctfTeamAllColumns,
			ctfTeamPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert ctf_teams, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(ctfTeamPrimaryKeyColumns))
			copy(conflict, ctfTeamPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"ctf_teams\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(ctfTeamType, ctfTeamMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ctfTeamType, ctfTeamMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert ctf_teams")
	}

	if !cached {
		ctfTeamUpsertCacheMut.Lock()
		ctfTeamUpsertCache[key] = cache
		ctfTeamUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CTFTeam record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CTFTeam) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CTFTeam provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ctfTeamPrimaryKeyMapping)
	sql := "DELETE FROM \"ctf_teams\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ctf_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ctf_teams")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ctfTeamQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ctfTeamQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ctf_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ctf_teams")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CTFTeamSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ctfTeamBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ctfTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"ctf_teams\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, ctfTeamPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ctfTeam slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ctf_teams")
	}

	if len(ctfTeamAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CTFTeam) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCTFTeam(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CTFTeamSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CTFTeamSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ctfTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"ctf_teams\".* FROM \"ctf_teams\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, ctfTeamPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CTFTeamSlice")
	}

	*o = slice

	return nil
}

// CTFTeamExists checks if the CTFTeam row exists.
func CTFTeamExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"ctf_teams\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ctf_teams exists")
	}

	return exists, nil
}

// Exists checks if the CTFTeam row exists.
func (o *CTFTeam) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CTFTeamExists(ctx, exec, o.ID)
}
